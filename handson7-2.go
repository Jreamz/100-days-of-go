package main

import "fmt"

func main() {
	oneHundred := 100
	daysOfCode := "days of code"
	daysRemaining := 93.0

	fmt.Printf("%v days of Golang! %v days remaining \n", oneHundred, daysRemaining)
	fmt.Printf("%v is of type %T \n", oneHundred, oneHundred)
	fmt.Printf("%v is of type %T \n", daysOfCode, daysOfCode)
	fmt.Printf("%v is of type %T \n", daysRemaining, daysRemaining)
}

/*
write simple program to:
for a VARIABLE storing a VALUE of TYPE string, int, float64
use print verbs to show VALUE > TYPE
*/
